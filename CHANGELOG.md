## v2.0.22 / 20260910
- 「复制格式」分组改名并按属性重整为三个子分组：
  1) 形状 / 文字 / 组合：每行 = 只读属性标签（图标+文字）+ 复制 + 粘贴（复制/粘贴统一为纯文本按钮，完整功能名保留在 ScreenTip；形状/文字/组合的“全部/填充/轮廓”等子菜单原样保留）；
  2) 位置：复制位置 / 粘贴位置 / 交换位置 原样保留；
  3) 宽度 / 高度 / 裁剪：与子分组 1 同构。
  分组更名为「格式属性」（对比“复制属性”更准确：不只复制，还含粘贴、交换与样式/尺寸/位置/裁剪等全部属性）。
- “设置间距”（对齐间距设置）弹窗继续加固：状态文字标签限制最大宽度，避免超长加载信息把弹窗撑宽错位；app.config 显式开启 WinForms 高 DPI 自动缩放（EnableWindowsFormsHighDpiAutoResizing + PerMonitorV2），与各弹窗的 96 DPI 基准缩放配合生效。
- 回归检查：`tests/Run-TitleRibbonLayout.ps1` 154 项通过（新增「格式属性」分组 29 项断言：子分组结构、只读标签、纯文本复制/粘贴、位置列原样、序列化 XML），`tests/Run-CoreRegression.ps1` 26 项通过。
- 说明：静态 customUI 预览渲染管线对 VSTO SplitButton 的序列化转换存在已知缺口（预览用文件渲染不出带 SplitButton 的分组），生产加载项由 VSTO 运行时序列化，同类结构（复制位置菜单）已在 2.0.21 实装验证可正常显示；「格式属性」真实观感待用户安装 2.0.22 后人工验收。
- 生成 2.0.22.0 安装包。

## v2.0.21 / 20260910
- 「导出与选择」分组从 SciFigure 功能区移到 SciStudio 功能区（位于“科研文本”之后、“关于”之前），SciFigure 减少一组、避免右侧被窗口裁掉。
- 逐项梳理该分组五个功能并补齐按钮体验：导出页面（修正原本错误的提示“将选中的图片导出为文件”→ 实际是导页面，新增 SuperTip）、导出原图（新增 FileSave 图标与说明）、复制大图（新增 Copy 图标与说明）、图文同缩（新增 PictureCompress 图标与说明）、全选文本框（补充 SuperTip）；五个按钮全部带图标，风格统一。
- 修复弹窗文字截断/压缩问题（根因：弹窗按 96 DPI 硬编码像素排版，而字体随显示器 DPI（200%）放大、控件框不放大）：导出设置弹窗与「图文同缩」「对齐间距设置」两个窗口统一改用 `ScientificUiTheme` 的 96 DPI 基准自动缩放（`AutoScaleMode.Dpi + AutoScaleDimensions(96,96)`，控件全部就位后装备缩放，与「局部放大」弹窗同款方案）。
- 回归检查：`tests/Run-TitleRibbonLayout.ps1` 125 项通过（新增导出分组所在选项卡、图标、提示、序列化 XML 断言），`tests/Run-CoreRegression.ps1` 26 项通过；真实 PowerPoint 渲染确认五个按钮图标全部显示、行列与按钮宽度统一；图标 ID 先用等效 customUI 渲染探测（Copy / FileSave / PictureCompress / TextBoxInsert 有效，FitToScreen / ExportSlides 无效弃用）。
- 生成 2.0.21.0 安装包；弹窗在用户 200% 缩放下的人工验收待安装后进行。

## v2.0.20 / 20260910
- 按用户真实安装后的反馈继续收敛「添加图片标签」分组布局：
  1. 移除两列之间的分割线，三列直接由 group 原生列排布；
  2. 字号 / 模板 / 编号与「图片自动排列」的“列数量”列完全同构——三个裸 ComboBox 直接进 group，实测行位置与“列数量”逐像素一致（164/216/268，行距 52/52），不再有纵向盒压紧造成的 5px 级差异；
  3. 垂直 / 水平偏移的输入框加宽为 `000000`（输入区 92 + 下拉箭头），使“标签文字 + 间隙 + 输入框”的总宽度与第三行两个切换按钮完全相等（实测左右边界同为 -30639，行总宽 236），并消除此前输入框被推到列右端、文字与输入框之间留出约 39px 大空档的问题（现约 18px，比「添加图片标题」偏移行的 25px 更紧凑）。第三行仍用横向盒容纳加粗、编号自动更新两个切换按钮（横向盒天然高出 4px，与「添加图片标题」一致）。
- 回归检查：`tests/Run-TitleRibbonLayout.ps1` 111 项通过（标签分组改为 9 个直接子项断言：无分割线、无纵向盒、仅第三行一个横向盒、输入宽度与预设），`tests/Run-CoreRegression.ps1` 26 项通过；真实 PowerPoint 渲染 + UI Automation 逐控件矩形核对上述三项结论。
- 生成 2.0.20.0 安装包：`F:\SlideSCI\artifacts\inno\SlideSCI-Focus-2.0.20.0-Setup.exe`；安装与真实 Office 冷启动验收待用户完成。

## v2.0.19 / 20260910
- 「添加图片标签」的垂直偏移 / 水平偏移由输入框改为与「添加图片标题」一致的 ComboBox：外观（输入框 + 下拉箭头）与预设下拉数值（-20/-10/-5/0/5/10/20）完全统一，仍可手动输入任意数值。
- 偏移输入宽度调整为 `0000`（与“列数量”同宽），真实渲染实测右边界与第三行两个切换按钮的右边界完全对齐（均为 1405 物理像素）；标签文字仍保留 thin space + hair space 内缩。
- 回归检查：`tests/Run-TitleRibbonLayout.ps1` 108 项通过（新增偏移控件类型、输入宽度与预设断言），`tests/Run-CoreRegression.ps1` 26 项通过；真实 PowerPoint 渲染 + UI Automation 核对第三列两行为 1311/1405（94 物理像素控件），与第二列及“列数量”一致。
- 按用户要求随后生成 2.0.19.0 安装包：`F:\SlideSCI\artifacts\inno\SlideSCI-Focus-2.0.19.0-Setup.exe`（3,486,176 字节，SHA-256 9F540B8444B76A8C39CC9AF57EFE6773305214B88AC0B68A9B20DFB6F87ED589），signtool 用本地开发证书成功签名（Authenticode 链终止于本机自签根证书，非公共信任链）。构建前检查了机器上 2.0.13/2.0.14 时代的 ClickOnce 遗留（D:\Program Files (x86)\SlideSCI Focus、VSTO Inclusion/SolutionMetadata 旧路径），2.0.15 起 Inno 脚本在安装时统一清理并只注册当前直接部署布局；安装与真实 Office 冷启动验收仍待进行。

## v2.0.18 / 20260910
- 「添加图片标签」分组重排为三列 × 三行，沿用「添加图片标题」已验证的行结构：每列前两行保持裸控件（原生行距），第三行用横向 RibbonBox，使三列第三行落在同一条基线上。
- 第一列：添加标签 / 更新标签 由 Large 改为 Regular 小按钮，左图标右文字，样式与“添加上标题”一致；第三行“字体”下拉框宽度改为 `0000`，使该行总宽度等于上面两个按钮（真实渲染实测 160 / 163 物理像素）。
- 第二列：字号 / 模板 / 编号统一为可输入 ComboBox，输入宽度 `0000`，与「图片自动排列」的“列数量”输入框完全一致（实测输入框 64、整控件 94 物理像素）；编号由 EditBox 改为 ComboBox，新增 1–20 预设，仍可手动输入任意编号。
- 第三列：X偏移 / Y偏移更名为垂直偏移 / 水平偏移；加粗、编号自动更新由 CheckBox 改为 ToggleButton（无图标、按下表示开启）；前两行输入宽度 `000000`，使输入框右边界与第三行两个切换按钮的右边界对齐（实测 1403 / 1405 物理像素）。
- 六个标签加入 thin space + hair space 内缩，与相邻分组的标签左端对齐；添加标签 / 更新标签补充 ScreenTip 与 SuperTip。
- 回归检查：Release 构建通过；`tests/Run-TitleRibbonLayout.ps1` 105 项通过（新增标签分组列结构、控件类型、输入宽度与序列化 XML 断言）；等价 customUI 注入 `.pptm` 后在真实 PowerPoint 中渲染，并用 UI Automation 读取控件矩形核对：三行行位置 164/216/264 与「添加图片标题」逐像素一致，第二列输入宽度与“列数量”相同。
- 本轮按要求不生成新安装包、不安装新版本；真实加载项下的按钮点击、下拉列表与切换状态仍需人工验收。

## v2.0.17 / 20260910
- 「添加图片标题」移除第三列外层纵向 RibbonBox，标题、字体和字号/对齐/编组复合行直接交给 group 排布，目标为恢复“列数量”列使用的原生宽行距，而不是继续将三行压紧。第三行内部仍保持字号、对齐、编组的顺序。
- 更正此前高度解释：3×48 + 2×4 = 152px，“需要 156px、物理上放不下”的推断错误；2.0.16 经用户反馈只能统一紧凑行距，未满足宽行距要求。
- 回归检查实际 Release 对象及 XML 的直属父级、控件顺序与旧纵向容器移除。未使用 UI 自动化；新版本实际行距、跨列基线与是否换列仍需人工 PowerPoint 验收，结构检查不代表视觉结果已通过。

## v2.0.16 / 20260910
- 「添加图片标题」第三列的标题、字体、字号/对齐/编组三行统一使用横向 RibbonBox，消除前两行裸控件与第三行容器混排的结构差异；第三行仍保留三个控件的原顺序，不增加第四列。
- 保留前两列、输入宽度、标题历史与事件绑定；更新实际 Release Ribbon 对象及序列化 XML 回归，检查第三列恰好三个横向行。
- 此改动统一第三列内部的布局方式，不宣称与裸控件组成的“列数量”列像素级一致。历史记录中的 152px/4px 是特定环境的测量，不是通用物理上限；未进行 UI 自动化或新版本人工视觉验收。

## v2.0.15 / 20260909
- 修复 2.0.14 发布后安装链路混用 ClickOnce `.vsto/setup.exe` 与 Inno 直部署导致的 `AddInAlreadyInstalledException`：最终 Inno 打包强制使用当前 `SlideSCI/bin/Release`，校验直接 VSTO 清单身份/版本/布局，并在中间产物与最终安装目录写入明确的使用提示。
- Inno 安装器清理本 fork 遗留的 ClickOnce VSTO 卸载项、VSTA 方案与 PowerPoint AddinsData 后，再注册唯一的 `CaptainMusX.SlideSCI.Focus` 加载项；不触碰其他插件。

## v2.0.14 / 20260909
- 「添加图片标题」三列改为与「图片自动排列」的“列数量”列相同的行结构：第一、二行为裸按钮/裸 ComboBox（原生 4 像素行距，行高与列数量一致），第三行保留横向容器容纳字号、对齐与编组三个控件；三列行位置在真实 PowerPoint 渲染中逐像素核对（第 1、2 行与列数量完全对齐，第 3 行比列数量高 4 物理像素，这是 152 像素列高下“三行 + 两个 4 像素间距”的物理上限）。
- 「局部放大」的“尺寸/间距”标签与“框线/连线”标签加入不可见空格（thin space + hair space），使标签控件与同列按钮/菜单/切换按钮的最左侧文字在视觉上左对齐；「添加图片标题」的偏移与标题/字体/字号标签同步处理。
- 「框线/连线」菜单按 PowerPoint 原生“形状轮廓”菜单补齐图标与预览：无轮廓（带斜杠方框）、其他轮廓颜色（原生调色盘图标 ShapeOutlineColorPicker）、取色器（吸管）、粗细（按磅值绘制的线条预览）、虚线（按线型绘制的虚线预览）、箭头（起点/终点箭头预览），并为无轮廓(N)、其他轮廓颜色(M)、虚线(S)设置与原生一致的加速键。
- 新增/更新 68 项 Ribbon 布局回归（行结构、图标、加速键、序列化 XML）与 26 项核心回归；真实 Office 的菜单弹层外观仍需人工验收。

## v2.0.13 / 20260909
- SciFigure/SciStudio 使用产品专属稳定选项卡 ID；演示文稿窗口激活时重新查询 SciFigure 可见性，不重置用户的 Office 自定义文件。启动缺失问题已复现，但新版本冷启动视觉验收尚未完成。
- 图片标题恢复三列，第三列第三行为完整字号 ComboBox、对齐菜单、编组；统一各列三行的容器深度，取消第四选项列。真实 Office 行高、上下边框位置仍需人工验收。
- 局部放大按钮更名为“框选区域”“放大选区”；两列尺寸使用相同输入宽度，百分比预设显示 %，倍数预设限定为 1/2/3/4/5/10x，默认 1x；手动输入可省略单位。
- 框线/连线使用独立标签与菜单，新增 Office Ribbon 主题色/标准色/最近色画廊、无轮廓、粗细、虚线、连线箭头预设及取色入口，设置应用于创建和更新路径。此菜单并非 PowerPoint 原生 Shape Outline 的完整复用，草绘为禁用项；精确等宽与取色交互需人工视觉验收。

## v2.0.12 / 20260908
- 「添加图片标题」第三列改为与「图片自动排列」的“列数量”列相同的三行 ComboBox 结构（标题 / 字体 / 字号），不再嵌套横向盒；第三行与其它分组第三行保持平行，相邻输入框的垂直间距一致。
- 字号由“EditBox + 独立预设菜单”合并为单个可编辑 ComboBox，输入框与下拉箭头不再分离，保留手动输入和全部预设字号。
- 「标题」ComboBox 下拉列表保存最近 5 次成功生成的标题，最新标题置顶，重复标题去重，超过 5 条淘汰最旧一条。
- 编组开关与对齐菜单移入独立的第四列，第三列三行保持纯 ComboBox 行高。
- 「局部放大」第一、二列的 ComboBox 标签加入 hair space（U+200A）微调，第一列“框线”菜单同步加一个 hair space，使同列文字左端对齐。

## v2.0.11 / 20260908
- 优化图片标题第三行：字号改为紧凑输入框与独立预设下拉菜单，保留手动输入、全部预设及设置保存，降低嵌套行的高度占用；编组和对齐方式仍在第三列第三行。
- 局部放大的尺寸、间距标签加入固定不换行空格，补偿输入标签与同列菜单/按钮的原生文本边距差异。
- 修复静态 Ribbon 预览写入无效容器属性的问题；更新对象和 XML 回归。最终版本未进行实际安装及 PowerPoint UI 验收。

## v2.0.10 / 20260908
- 修复「添加图片标题」多列回归：移除独立选项列及其分隔线，将字号、编组和对齐菜单恢复到第三列第三行。
- 布局回归检查实际 Release Ribbon 对象及其序列化 XML，要求恰好三列且三个选项位于同一水平行；真实 PowerPoint UI 验收另行进行。

## v2.0.9 / 20260908
- 「图片自动排列」的列数量、列间距、行间距、图宽、图高输入框宽度缩为原来的 2/3（sizeString 由 6 位改为 4 位；下拉箭头宽度固定，整体控件约为原来的 3/4）。
- 「添加图片标题」的上下偏移/左右偏移更名为垂直偏移/水平偏移，ScreenTip、SuperTip 与参数报错文案同步更新。
- 修复「添加图片标题」第三列第三行不平行：横向 RibbonBox 比普通控件高约 4px，作为垂直盒最后一行会被 PowerPoint 向上挤压，使“字号”与上方“字体”之间只剩约 1px 间距；现改为每行只放一个控件，编组开关与对齐方式移入独立的第四列，四列行高与其它分组一致。
- 「局部放大」按功能相关度重排为三列：选区/尺寸/框线、放大/尺寸/引线、间距/连线/编组；编组改为切换按钮，菜单标签宽度固定，切换选项不再引起整组重新排版。
- 布局回归测试重写为 40 项：覆盖输入宽度、四列/三列结构、无横向盒、偏移更名与局部放大分组顺序；用等价 XML 在真实 PowerPoint 中渲染并按像素核对行距（三行间距均为 5px）。

## v2.0.8 / 20260908
- 修复真实 PowerPoint 渲染：去掉第三列的中间垂直槽（titleFormattingRowSlot），字号/编组/对齐方式水平行直接作为第三列的第三行，不再被 PowerPoint 渲染成第四列。
- 第三列宽度基准保持为第三行“字号+编组+对齐方式”合计宽度；标题/字体输入框继续使用同一加宽宽度，字号输入与上下/左右偏移保持同一窄宽度。
- 布局回归测试新增断言：第三列内不再包含会外溢成新列的垂直包装盒，序列化 Ribbon 顶层仍固定为三列。

## v2.0.7 / 20260908
- 修复图片标题三列层级：第三列恢复为单一垂直列，字号/编组/对齐方式保持在第三列第三行，不再外溢成新列。
- 上下与左右标题列统一为相同按钮尺寸、相同偏移输入宽度和相同图标；左右标题复用上下标题图标。
- 标题和字体输入统一为相同类型与宽度，字号输入与上下/左右偏移使用同一窄宽度；第三行宽度作为第三列宽度基准。

## v2.0.6 / 20260908
- 添加图片标题分组的三列之间加入 Ribbon 间隔线，第三列的标题输入框与字体下拉框统一对齐宽度。
- 第三列第三行独立布局，字号、编组和对齐菜单与前两行保持稳定的纵向间距。
- 对齐菜单改用普通按钮，当前对齐方式仍显示在折叠菜单图标上，展开选项不再出现选中方框。

## v2.0.5 / 20260908
- 图片标题分组改为三列：上下标题与上下偏移、左右标题与左右偏移、标题/字体/字号及编组开关和图标对齐菜单。
- 新增横排左右侧标题；上下/左右偏移默认为 0 pt，分别正向向下/向右，同时叠加作用于四个方向。
- 上标题依据实际文字高度定位；四种原生段落对齐图标同步当前选择，编组失败保留图片和标题。
- 新偏移独立保存，不将旧“图下距”的相反方向语义迁入；保留原字体、标题、编组及居中偏好。

## v2.0.4 / 20260908
- 📦 明确代理开发流程：每轮实际改进必须递增版本、完成回归、生成新的 Release/Inno 安装包，并将本轮 commit 推送到 GitHub；交付时记录安装包路径、SHA-256 和验证边界。
- 📝 新增根目录 `AGENTS.md`，同步更新贡献与构建文档，避免只留下本地修改或只报告构建命令。

## v2.0.3 / 20260907
- 局部放大设置迁入三列三行 Ribbon：选择区域、生成、编组、选区预设、放大尺寸、pt 图间距、连线方式及独立线条样式菜单。
- 移除透明锚点与内部组合；图片和选区框使用包含四角与边中点的原生连接点，保留原图像像素与裁剪信息。
- 默认不编组；启用时仅创建单层组合。更新先生成新结果再替换旧结果，支持选中放大图或引线定位选区。
- 引线方向跟随放大图布局；提供平行、交叉、无连线，选区框与引线分别支持实线、虚线、点线、点划线。

## v2.0.2 / 20260905
- 修复 LaTeX 转换在输入写入阶段阻塞时无法触发超时的问题，统一限制输入、输出和退出等待，并保留子进程错误信息。
- 放大图设置在读取和窗口初始化时校验非有限值、范围和枚举；保存采用同目录临时文件原子替换，失败明确提示。
- Windows 构建显式使用 npm.cmd，避免解析到不兼容的 PowerShell 包装脚本。

## v2.0.1 / 20260904
- 🐛 修复局部放大设置窗口在 Windows 200% 缩放下文字重叠、控件截断和按钮显示不完整的问题
- 🎨 代码构建型窗口在全部控件完成布局后再应用 96 DPI 设计基线，兼容 100%–200% 缩放

## v2.0.0 / 20260904
- 🔖 插件正式更名为「SlideSCI Focus」，程序集与 VSTO 加载项身份迁移为 `CaptainMusX.SlideSCI.Focus`
- 🔧 Ribbon 标签页重命名为 `SciFigure` 与 `SciStudio`，保持 Figure 工作流与通用工具的功能边界
- 📜 补充修改版来源、AGPL-3.0 和发布源代码说明；保留原作者版权与免责声明

## v1.9.4 / 20260904
- 🐛 修复局部放大设置窗体因自绘控件透明背景初始化异常而无法打开的问题

## v1.9.3 / 20260827
- 📦 精简：移除 AI 助手与素材库侧边栏及相关依赖（Newtonsoft.Json、CSharpExecutor、DPAPI 配置）
- ✨ 新增「局部放大」：期刊风格局部放大图（插入选区框 → 生成放大图），目标尺寸支持与原图相同/指定倍数/自定义宽度，连线支持期刊漏斗（框下两角→放大图上两角）与交叉 X 形，可整体编组，重复生成自动覆盖
- 🔗 连线连接点吸附：连线端点粘附到选区框四角与放大图四角（隐形角锚点），移动图片/选区框时连线自动跟随拉伸，无需重算坐标
- 🎨 生成参数对话框：连线/框线颜色（含自定义取色）、连线线型（实线/虚线）、放大图尺寸与放大倍数实时预览、比例不一致拉伸警告
- 🎨 参数弹窗重绘为 Apple 风格：圆角卡片分区、iOS 蓝实心主按钮、两列栅格对齐、预览卡自动换行，修复文字截断
- 🧪 局部放大修复：裁剪改为按 PowerPoint 原始图片磅值计算，支持已有裁剪；多选区独立更新，透明角锚点真实参与连接，自动避开幻灯片边界
- 🎨 局部放大 UI：改为 Windows DPI 自适应的科研工具风格，使用数值控件、颜色色块、键盘焦点和明确的图像比例提示
- ✨ 交互重做：「生成放大图」单击直接沿用上次设置生成（首次自动打开设置），Ctrl+单击随时打开「局部放大设置」，设置自动记忆
- 🎨 设置弹窗重做：分段控件选择目标/连线、模式联动输入、实时示意图预览（原图/选区/放大图/引线）、键盘方向键与焦点框
- 🐛 修复旧版本遗留物导致「局部放大失效」：升级后首次生成自动清扫旧格式放大图/连线并把选区框迁移到新命名，重生成不再叠层残留；选区框默认改为黑色描边（已用真实 PowerPoint COM 端到端验证裁剪/粘附/编组链路）
- 📝 README 重写为与当前功能一致
- 🔧 Ribbon 新增「局部放大」组（Tab1 SlideSCI，图片自动排列之后）

## v1.9.2 / 20260826
- 🐛 修复自动排列输入、Markdown 转换、组格式粘贴和素材库文件操作的稳定性问题
- 🔒 AI API Key 使用 Windows DPAPI 保存，执行 AI 生成的 C# 代码前增加确认
- 🔧 改进 LaTeX 转换超时处理、PowerPoint COM 清理和 VSTO 发布流程
- 📦 安装器改为直接 VSTO 布局，并使用 `CaptainMusX.SlideSCI` 独立加载项身份，清理原版和旧测试版的 ClickOnce 元数据；默认不捆绑 MathJax 依赖

## v1.9.1 / 20260816
- 🎨 添加赞助商APIMart

## v1.9 / 20260816
- 🎨 图片排列的宽高单位设置为cm
- 🎨 字体支持显示系统已安装字体

## v1.8 / 20260610
- ✨ 复制粘贴组样式
- ✨ 新增间距设置
- ✨ 实现真正的图文同缩


## v1.7.1 / 20260610
- ✨ 新增交换位置
- 🎨 素材库：支持切换视图
- 🎨 素材库：插入素材默认往屏幕中心插入
- 🎨 素材库：支持更新素材

## v1.7 / 20260605
- ✨ 支持AI侧边栏，AI编辑PPT功能

## v1.6 / 20260605
- ✨ 支持素材库


## v1.5 / 20260604
- 🎨 新增水平居中和垂直居中按钮，默认以选中的第一个对象为参考
- 🎨 复制粘贴格式升级，区分文本格式和形状格式
- 🎨 复制位置支持选择位置
- 🎨 支持全选页面文本框 [#67](https://github.com/Achuan-2/SlideSCI/issues/67)
- 🎨 插入latex文字优化 [#61](https://github.com/Achuan-2/SlideSCI/issues/61)
- 🎨 支持含有markdown的文本框直接转富文本
- 🎨 插入Markdown多个段落时自动组合
- 🎨 插入Markdown支持粘贴SVG


## v1.4 / 20251129

- 🎨任何对象都可以添加图片标题
- 🎨子图标签设置times new roman字体修复 [#53](https://github.com/Achuan-2/SlideSCI/issues/53)

## v1.3 / 20250929
- ✨支持插入latex svg，以解决插入latex文字版本有些latex语法不支持的问题
  - 配置方法（需要node.js环境）
    - 配置Node.js环境
    - 进入插件安装文件夹的latex-converter文件夹
    - 运行`npm install`安装

## v1.2 / 20250929

  - 🐛 fix(数学公式): 修复插入Markdown，$$没有去除的问题


## v1.1 / 2025.09.24

- ✨图片添加标签新增计数器、更新标签文本功能 [#36](https://github.com/Achuan-2/SlideSCI/issues/36)
- ✨图片标签-增加更新标签功能 [#42](https://github.com/Achuan-2/SlideSCI/issues/42)
- ✨图片标签：标签模板新增(a)和(A) [#44](https://github.com/Achuan-2/SlideSCI/issues/44)
- ✨图片标签添加Time new Roman字体

## v1.0.2.1 / 2025.07.09
  - ✨ feat(复制大图): 复制大图支持占位符图片


## v1.0.2.0 / 2025.07.09
- 🐛fix(图片标题): 图片通过占位符粘贴，无法添加标题

## v1.0.1.9 / 2025.07.08

- ✨添加图片标题优化
  - 添加图片标题支持添加上标题
  - 图片标题支持设置居中或居左

![](https://fastly.jsdelivr.net/gh/Achuan-2/PicBed/assets/PixPin_2025-07-07_17-49-34-2025-07-07.png)

## v1.0.1.8 / 2025.07.07
- ✨ PPT便捷导出原图
- ✨复制原图更名为复制大图


## v1.0.1.7 / 2025.07.07
- ✨ PPT复制原始图片到剪贴板 [#32](https://github.com/Achuan-2/SlideSCI/issues/32)
  - 实现方案：**直接复制形状+临时调整尺寸**

    1. 不用export函数+临时文件复制方案，直接用shape copy复制到剪贴板
    2. 不设置scale百分比为100，改为临时设置图片高度为幻灯片高度，复制之后，再调整回之前的宽高，这样虽然不是原始图片，但是也能用

    为什么选择这个方案

    - 设置这个功能就是方便自己从ppt复制图片到笔记软件，方便整理，不需要追求原始图片保存，真要保存，可以用ppt自带的复制原始图片功能
    - 这个方案比较实用，比如512*512的tif，如果是原始图片直接复制就太小了，粘贴到笔记软件不方便查看，更别说很多笔记软件并不支持显示tif，一般图片调整到幻灯片高度都是能看的
- ✨ feat(添加标题): 支持给视频添加标题
- ✨ feat(代码块高亮）：支持R语言代码高亮 [#31](https://github.com/Achuan-2/SlideSCI/issues/31)
- ✨ feat(添加标题): 支持给视频添加标题

## v1.0.1.6 / 2025.05.22

- ✨ feat(导出功能): 统一导出页面和选中页面的命名


## v1.0.1.5 / 2025.05.19
- ✨ feat(导出功能): 添加PDF导出格式
- ✨ feat(导出功能): 支持多选页面批量导出为图片和pdf


# v1.0.1.4 / 2025.05.19
- ✨ feat(导出功能): 添加图片导出设置对话框
  - 实现导出范围、格式和DPI选择
  - 添加导出后打开文件夹的选项
  - 处理无演示文稿和无幻灯片的情况


## v1.0.1.3 / 2025.05.04
- 🐛 fix(列表转换): 修复有序列表编号保存问题 [#27](https://github.com/Achuan-2/SlideSCI/issues/27)
  - 保持列表编号项目符号类型（之前会把ABCD的样式变为1234）
  - 保存列表编号起始值（之前会把开头为序号2的列表改为序号1）
- 🐛 fix(列表转换): 避免列表样式受后续字体样式干扰

## v1.0.1.2/ 2025.05.04
- 🐛fix (公式处理): 修复行内数学公式插入吞字问题 [#26](https://github.com/Achuan-2/SlideSCI/issues/26)

## v1.0.1.1 / 2025.05.04

- ✨ feat(图片添加标题)：添加标题终于不再受默认文本框格式的影响，始终居中，文本框宽度与图片宽度一致，自动换行
- ✨ feat(insertMarkdown): 粘贴md位置改进，插入位置改为幻灯片中间
- 📝 添加赞赏按钮


## v1.0.1.0 / 2025.04.16 
- 💄 UI(Tab): 新增一个Tab栏，操作更方便
- ✨ feat(添加图片标题): 图片标题功能优化自动编组，处理编组失败的情况
    - 原因：PPT复制到占位符框框产生的图片不能和其他图片、文字进行组合。
    - 解决方案：遇到这种情况可以把图片+标题复制一份，删除原图片+标题，粘贴拷贝的图片+标题到原位置，再进行组合，如果不行，再报错。
- ✨ feat(insertMarkdown): 粘贴md位置改进，插入位置改为PPT外侧


## v1.0.0.55 /2025.02.23 by [zixuan203344](https://github.com/zixuan203344) ❤
- ✨ 部分标签文字，在能理解其意思的基础上尽可能缩短，排列整齐
- ✨ 几乎所有的可输入值，改为下拉框，可下拉选择，可手动输入
- ✨ 列数量、列间距、行间距、图宽、图高等值的改变，会自动触发一次图片排列，可实时更改
- ✨ 图片加标题功能，增加开关，可自定义排除文本框、形状与否
- 🐛 图片加标题功能，修正打开编组功能后，只能增加2组标题，且编组只能编一次，现在编组功能正常，编组完成后，自动选择所有对象方便移动或同时缩放
- ✨ 字体增加下拉框，可选择常用的PPT字体（前提是PC上要安装这些字体）
- ✨ 格式复制粘贴，将常用的8组按钮移动到外面，方便使用

## v1.0.0.54 / 2025.02.15
- 🐛 fix：某些图片无法添加图片标题
- ✨ 图片和文字组合支持同步缩放 [#18](https://github.com/Achuan-2/SlideSCI/issues/18)
- ✨ 图片标签和标题添加时，自动全选中添加的标签和标题，方便修改

## v1.0.0.53 / 2025.01.24

- ✨ 图片排列的排列方式：最大宽度排列功能完善，改为按照列最大宽度，更加实用
- ✨ 图片标签支持自动加粗

## v1.0.0.52 / 2025.01.24

- 🐛 复制粘贴图片裁剪失效
- ✨ 图片排列的排列方式支持三种选项
- ✨ 图片排列支持设置是否排除文本框
- ✨ 图片标签支持数字编号、自动排除文本框和形状
- ✨ 现在插件的参数修改后，再打开能够保持之前的设置
- ✨ 复制粘贴位置效果完善：使用中心位置粘贴，适应形状大小不同的位置粘贴


## v1.0.0.50/2025.01.23

- 🎨 插件改名为SlideSCI
- 📝 添加未经允许，禁止商用
- ✨ 支持添加图片标签，用于科研绘图
- ✨ 支持复制粘贴格式：支持复制形状、文本框、选中部分文字格式并粘贴格式
- 💄 界面调整：Ribbon使用专用选项卡（之前是默认的，有问题，其他插件也可能占用）、并使用menu来包裹复制粘贴格式的系列按钮，节省选项卡空间

## v1.0.0.46/2025.01.20

- ✨ 图片自动排列：支持根据位置自动排列图片
  - 可以根据用户手动排列的图片位置，来自动判断图片的排列顺序

## v1.0.0.45/2025.01.17

- ✨ 插入markdown：行内数学公式支持分式
    需要临时插入块级数学公式，否则不会正常转化？

## v1.0.0.44/2025.01.17

- ✨ 获取当前插件版本号



## v1.0.0.43/2025.01.17

- ✨ 插入markdown，支持行间数学公式
- 

## v1.0.0.42/2025.01.17

- ✨ 添加帮助文档，跳转到语雀

## v1.0.0.41/2025.01.16
- ✨ 复制粘贴位置支持批量
- ✨ 插入Markdown：优化剪贴板写入，粘贴完之后最新的剪贴板为复制的全部内容，避免内容分块导致的剪贴板污染



## v1.0.0.40/ 2025.01.15
- ✨ 支持图片复制粘贴裁剪
- ✨ 支持打开GitHub repo页面
- ✨ 插入markdown
  - 默认字体设置为微软雅黑
  - 支持列表粘贴：默认添加悬挂缩进
  - 支持任务列表粘贴，支持转化为☑和☐
  - 支持引述块粘贴：独立文本框，添加黑色边框
  - 支持表格粘贴：默认限制500px宽度，添加1pt黑色边框
  - 支持直接插入公式块
  - 支持普通段落和代码块、公式块混排
  - 支持粘贴行内代码：调整为暗红色字体颜色


## v1.0.0.35 / 2025.01.13
- ✨支持安装到WPS（注：WPS不支持插入latex公式）

## v1.0.0.34 / 2025.01.13
- ✨支持插入latex数学公式

## v1.0.0.24 / 2025.01.13
- ✨支持代码块高亮，目前已经支持python、csharp、js、matlab、css、html等代码高亮，插入的代码块默认为黑色

##  v1.0.0.14 / 2025.01.13
- ✨添加开发者信息

## v1.0.0.11 / 2025.01.13

- ✨ 优化图片标题添加功能，支持图片标题自定义字体名称、字体大小、距离图片下边距离，默认标题文本、是否与图片自动编组
   ![](https://fastly.jsdelivr.net/gh/Achuan-2/PicBed/assets/
